<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sourcey.TimeTracker" >
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="AeriesTimeTracker"
        android:theme="@style/AppTheme" >

        <activity
            android:name="com.sourcey.TimeTracker.LoginActivity" android:theme="@style/AppTheme.Dark"
            android:label="AeriesTimeTracker" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity  android:name="com.sourcey.TimeTracker.MainActivity" />
    </application>

</manifest>
